import { useEffect, useMemo, useState } from 'react'
import type { ChangeEvent, FormEvent } from 'react'
import './App.css'
import type { DistributionMode, GeneratedResult, Player, TeamBalanceMode } from './types'
import { clearState, loadState, saveState } from './utils/storage'
import { generateTeams } from './utils/randomize'

const initialState = loadState()
const ratingCategories = [{ label: 'Beginner', min: 0 }, { label: 'Developing', min: 2 }, { label: 'Solid', min: 4 }, { label: 'Strong', min: 6 }, { label: 'Elite', min: 8 }]
function makePlayer(name: string): Player { return { id: crypto.randomUUID(), name: name.trim(), rating: 5, skillRating: null, preferredPosition: null } }
function ratingCategory(rating: number) { return [...ratingCategories].reverse().find((category) => rating >= category.min)?.label ?? 'Beginner' }

function App() {
  const [players, setPlayers] = useState<Player[]>(initialState?.players.map((player) => ({ ...player, rating: player.rating == null ? (player.quality ?? 3) * 2 : player.rating > 10 ? player.rating / 10 : player.rating })) ?? [])
  const [nameInput, setNameInput] = useState('')
  const [bulkInput, setBulkInput] = useState('')
  const [teamCount, setTeamCount] = useState(initialState?.teamCount ?? 2)
  const [mode, setMode] = useState<DistributionMode>(initialState?.mode ?? 'auto')
  const [playersPerTeam, setPlayersPerTeam] = useState(initialState?.playersPerTeam ?? 5)
  const [teamNames, setTeamNames] = useState<string[]>(initialState?.teamNames ?? ['', ''])
  const [matchName, setMatchName] = useState(initialState?.matchName ?? '')
  const [matchDate, setMatchDate] = useState(initialState?.matchDate ?? '')
  const [searchQuery, setSearchQuery] = useState('')
  const [captainMode, setCaptainMode] = useState<'none' | 'before' | 'after' | 'random'>(initialState?.captainMode ?? 'none')
  const [captainId, setCaptainId] = useState(initialState?.captainId ?? '')
  const [balanceMode, setBalanceMode] = useState<TeamBalanceMode>(initialState?.balanceMode ?? 'random')
  const [activeTab, setActiveTab] = useState<'match' | 'timer'>('match')
  const [timerMinutes, setTimerMinutes] = useState(initialState?.timerMinutes ?? 90)
  const [secondsLeft, setSecondsLeft] = useState((initialState?.timerMinutes ?? 90) * 60)
  const [timerRunning, setTimerRunning] = useState(false)
  const [result, setResult] = useState<GeneratedResult | null>(null)
  const [message, setMessage] = useState('')
  const [isShuffling, setIsShuffling] = useState(false)
  const [copied, setCopied] = useState(false)
  useEffect(() => { saveState({ players, teamCount, mode, playersPerTeam, teamNames, matchName, matchDate, captainMode, captainId, balanceMode, timerMinutes }) }, [players, teamCount, mode, playersPerTeam, teamNames, matchName, matchDate, captainMode, captainId, balanceMode, timerMinutes])
  useEffect(() => { if (!timerRunning) return; const interval = window.setInterval(() => setSecondsLeft((value) => { if (value <= 1) { setTimerRunning(false); return 0 } return value - 1 }), 1000); return () => window.clearInterval(interval) }, [timerRunning])

  const customUnassigned = mode === 'custom' ? Math.max(0, players.length - teamCount * playersPerTeam) : 0
  const canGenerate = players.length >= teamCount && (mode === 'auto' || playersPerTeam > 0)
  const visiblePlayers = players.filter((player) => player.name.toLocaleLowerCase().includes(searchQuery.toLocaleLowerCase()))
  const distribution = useMemo(() => { if (mode === 'custom') return `${playersPerTeam} per team`; const base = Math.floor(players.length / teamCount); return `${base + (players.length % teamCount ? 1 : 0)} vs ${base}` }, [mode, players.length, playersPerTeam, teamCount])

  const addNames = (raw: string) => {
    const names = raw.split(/[\n,]+/).map((name) => name.trim()).filter(Boolean)
    const existing = new Set(players.map((player) => player.name.toLocaleLowerCase())); const additions: Player[] = []; let duplicate = false
    names.forEach((name) => { const key = name.toLocaleLowerCase(); if (existing.has(key)) duplicate = true; else { existing.add(key); additions.push(makePlayer(name)) } })
    if (additions.length) setPlayers((current) => [...current, ...additions])
    setMessage(duplicate ? 'Some names were already on the roster.' : additions.length ? `${additions.length} player${additions.length === 1 ? '' : 's'} added.` : 'Enter a player name first.')
  }
  const handleAdd = (event: FormEvent) => { event.preventDefault(); addNames(nameInput); setNameInput('') }
  const updateTeamName = (index: number, value: string) => setTeamNames((current) => { const next = [...current]; next[index] = value; return next })
  const updatePlayerName = (player: Player, value: string) => {
    const normalized = value.trim()
    const duplicate = normalized && players.some((item) => item.id !== player.id && item.name.toLocaleLowerCase() === normalized.toLocaleLowerCase())
    if (duplicate) { setMessage(`${normalized} is already on the roster.`); return }
    setPlayers((current) => current.map((item) => item.id === player.id ? { ...item, name: value } : item))
  }
  const captain = players.find((player) => player.id === captainId)
  const randomize = () => {
    if (!canGenerate) { setMessage(players.length < teamCount ? `You need at least ${teamCount} players to create ${teamCount} teams.` : 'Check your team setup.'); return }
    if (captainMode === 'before' && !captainId) { setMessage('Choose a captain before randomizing.'); return }
    setIsShuffling(true); window.setTimeout(() => { try { setResult(generateTeams(players, teamCount, mode, playersPerTeam, teamNames, Math.random, balanceMode)); if (captainMode === 'random') setCaptainId(players[Math.floor(Math.random() * players.length)].id); setMessage('Teams are ready.') } catch (error) { setMessage(error instanceof Error ? error.message : 'Unable to generate teams.') } finally { setIsShuffling(false) } }, window.matchMedia('(prefers-reduced-motion: reduce)').matches ? 0 : 650)
  }
  const copyTeams = async () => {
    if (!result) return
    const text = ['FOOTBALL MATCH TEAMS', matchName ? matchName.toUpperCase() : '', matchDate ? new Date(`${matchDate}T00:00:00`).toLocaleDateString() : '', captain ? `CAPTAIN: ${captain.name}` : '', '', ...result.teams.flatMap((team) => [`${team.name} — ${team.players.length} players`, ...team.players.map((player, index) => `${index + 1}. ${player.name}${player.id === captainId ? ' (CAPTAIN)' : ''}`), '']), ...(result.unassigned.length ? ['UNASSIGNED', ...result.unassigned.map((player) => `• ${player.name}`)] : [])].join('\n')
    await navigator.clipboard.writeText(text); setCopied(true); window.setTimeout(() => setCopied(false), 1800)
  }
  const shareTeams = async () => { if (!result) return; const text = result.teams.map((team) => `${team.name}: ${team.players.map((player) => player.name).join(', ')}`).join('\n'); if (navigator.share) await navigator.share({ title: 'KickSplit teams', text }); else await copyTeams() }
  const reset = () => { if (!window.confirm('Start a new match and clear this roster?')) return; clearState(); setPlayers([]); setResult(null); setTeamNames(['', '']); setMatchName(''); setMatchDate(''); setCaptainMode('none'); setCaptainId(''); setBalanceMode('random'); setMessage('New match started.') }
  const formatTime = (seconds: number) => `${String(Math.floor(seconds / 60)).padStart(2, '0')}:${String(seconds % 60).padStart(2, '0')}`
  const chooseTimerDuration = (minutes: number) => { setTimerMinutes(minutes); setSecondsLeft(minutes * 60); setTimerRunning(false) }
  const resetTimer = () => { setTimerRunning(false); setSecondsLeft(timerMinutes * 60) }
  const onNumberChange = (event: ChangeEvent<HTMLInputElement>, setter: (value: number) => void) => { const value = Number(event.target.value); if (Number.isFinite(value)) setter(Math.max(1, Math.min(99, value))) }

  return <div className="app-shell">
    <header className="topbar"><a className="brand" href="/"><span className="brand-mark">⚽</span><span><strong>KickSplit</strong><small>Football team randomizer</small></span></a><button className="text-button" onClick={reset}>Start new match</button></header>
    <main>
      <nav className="app-tabs" aria-label="App sections"><button className={activeTab === 'match' ? 'active' : ''} onClick={() => setActiveTab('match')}>Team builder</button><button className={activeTab === 'timer' ? 'active' : ''} onClick={() => setActiveTab('timer')}>Match timer</button></nav>
      {activeTab === 'timer' ? <section className="timer-view"><div className="eyebrow">MATCH DAY / TIMER</div><h1>Keep the game<br /><em>moving.</em></h1><p className="timer-intro">Set the duration, start the clock, and stay focused on the match.</p><div className={`timer-display ${secondsLeft === 0 ? 'finished' : ''}`}><span>{formatTime(secondsLeft)}</span><small>{secondsLeft === 0 ? 'Full time' : timerRunning ? 'Match in progress' : 'Ready to start'}</small></div><div className="timer-presets"><span>Duration</span><div>{[30, 45, 60, 90].map((minutes) => <button className={timerMinutes === minutes ? 'selected' : ''} key={minutes} onClick={() => chooseTimerDuration(minutes)}>{minutes} min</button>)}<label className={timerMinutes !== 30 && timerMinutes !== 45 && timerMinutes !== 60 && timerMinutes !== 90 ? 'custom-timer selected' : 'custom-timer'}><input aria-label="Custom timer duration in minutes" type="number" min="1" max="240" value={timerMinutes} onChange={(event) => chooseTimerDuration(Math.max(1, Math.min(240, Number(event.target.value) || 1)))} /> min</label></div></div><div className="timer-actions"><button className="primary-cta" onClick={() => setTimerRunning((value) => !value)}>{timerRunning ? 'Pause timer' : secondsLeft === 0 ? 'Start again' : 'Start timer'} <span>{timerRunning ? 'Ⅱ' : '▶'}</span></button><button className="timer-reset" onClick={resetTimer}>Reset clock</button></div><p className="timer-note">The timer stays on this device and does not affect your team draw.</p></section> : <>
      <section className="intro"><div className="eyebrow">MATCH DAY / QUICK SETUP</div><h1>Fair teams.<br /><em>Fast setup.</em></h1><p>Add the players who showed up, set your format, and get a clean random draw in seconds.</p></section>
      <section className="session-bar"><div className="session-label"><span className="step">SESSION</span><strong>{matchName || 'Untitled match'}</strong><small>{matchDate ? new Date(`${matchDate}T00:00:00`).toLocaleDateString(undefined, { weekday: 'short', month: 'short', day: 'numeric' }) : 'Add a name and date for this match'}</small></div><div className="session-fields"><label className="sr-only" htmlFor="match-name">Match name</label><input id="match-name" value={matchName} onChange={(event) => setMatchName(event.target.value)} placeholder="Match name" /><label className="sr-only" htmlFor="match-date">Match date</label><input id="match-date" type="date" value={matchDate} onChange={(event) => setMatchDate(event.target.value)} /></div></section>
      <div className="workspace">
        <section className="panel roster-panel"><div className="panel-heading"><div><span className="step">01</span><h2>Players</h2></div><span className="count-pill">{players.length} {players.length === 1 ? 'player' : 'players'}</span></div>
          <form className="add-row" onSubmit={handleAdd}><label className="sr-only" htmlFor="player-name">Player name</label><input id="player-name" value={nameInput} onChange={(event) => setNameInput(event.target.value)} placeholder="Enter a player name" /><button className="primary-small" type="submit">Add player <span>+</span></button></form>
          <div className="bulk-row"><label className="sr-only" htmlFor="bulk-names">Bulk player names</label><textarea id="bulk-names" value={bulkInput} onChange={(event) => setBulkInput(event.target.value)} placeholder="Paste multiple names, one per line or comma-separated" rows={2} /><button className="secondary-button" type="button" onClick={() => { addNames(bulkInput); setBulkInput('') }}>Add list</button></div>
          {message && <p className="status" aria-live="polite">{message}</p>}
          {players.length > 6 && <div className="roster-search"><label className="sr-only" htmlFor="roster-search">Search players</label><input id="roster-search" value={searchQuery} onChange={(event) => setSearchQuery(event.target.value)} placeholder="Search roster" /><span>{visiblePlayers.length}/{players.length}</span></div>}
          <div className="roster-list" aria-live="polite">{players.length === 0 ? <div className="empty-roster"><span>✦</span><strong>No players yet</strong><p>Add everyone available for today's match.</p></div> : visiblePlayers.length === 0 ? <div className="empty-roster"><strong>No matching players</strong><p>Try a different search.</p></div> : visiblePlayers.map((player) => <div className="player-row" key={player.id}><span className="player-number">{String(players.indexOf(player) + 1).padStart(2, '0')}</span><input aria-label={`Edit ${player.name}`} value={player.name} onChange={(event) => updatePlayerName(player, event.target.value)} onBlur={(event) => { if (!event.target.value.trim()) setPlayers((current) => current.filter((item) => item.id !== player.id)) }} /><label className="player-rating"><span>{ratingCategory(player.rating)}</span><input aria-label={`Points for ${player.name}, 0.0 to 10.0`} type="number" min="0" max="10" step="0.1" value={player.rating} onChange={(event) => setPlayers((current) => current.map((item) => item.id === player.id ? { ...item, rating: Math.max(0, Math.min(10, Math.round((Number(event.target.value) || 0) * 10) / 10)) } : item))} /></label><button className="remove-button" aria-label={`Remove ${player.name}`} onClick={() => setPlayers((current) => current.filter((item) => item.id !== player.id))}>×</button></div>)}</div>
          {players.length > 0 && <button className="clear-button" onClick={() => window.confirm('Clear all players?') && setPlayers([])}>Clear all players</button>}
        </section>
        <section className="panel setup-panel"><div className="panel-heading"><div><span className="step">02</span><h2>Team setup</h2></div></div><div className="field"><label htmlFor="team-count">Number of teams</label><div className="stepper"><button aria-label="Decrease teams" onClick={() => setTeamCount((value) => Math.max(2, value - 1))}>−</button><input id="team-count" type="number" min="2" max="99" value={teamCount} onChange={(event) => onNumberChange(event, setTeamCount)} /><button aria-label="Increase teams" onClick={() => setTeamCount((value) => Math.min(99, value + 1))}>+</button></div></div><fieldset className="mode-field"><legend>Distribution</legend><label className={mode === 'auto' ? 'radio-card selected' : 'radio-card'}><input type="radio" checked={mode === 'auto'} onChange={() => setMode('auto')} /> <span><strong>Auto balance</strong><small>Everyone gets assigned, as evenly as possible</small></span><i>✓</i></label><label className={mode === 'custom' ? 'radio-card selected' : 'radio-card'}><input type="radio" checked={mode === 'custom'} onChange={() => setMode('custom')} /> <span><strong>Custom team size</strong><small>Choose the exact players per team</small></span><i>✓</i></label></fieldset>{mode === 'custom' && <div className="field custom-size"><label htmlFor="players-per-team">Players per team</label><input id="players-per-team" type="number" min="1" max="99" value={playersPerTeam} onChange={(event) => onNumberChange(event, setPlayersPerTeam)} /><p>{customUnassigned > 0 ? `${customUnassigned} player${customUnassigned === 1 ? '' : 's'} will remain unassigned.` : 'All players fit this format.'}</p></div>}<div className="field team-names"><label>Team names <small>Optional</small></label>{Array.from({ length: teamCount }, (_, index) => <input key={index} value={teamNames[index] ?? ''} placeholder={`Team ${index + 1}`} onChange={(event) => updateTeamName(index, event.target.value)} />)}</div><fieldset className="captain-field"><legend>Captain</legend><label className={captainMode === 'none' ? 'radio-card selected' : 'radio-card'}><input type="radio" checked={captainMode === 'none'} onChange={() => { setCaptainMode('none'); setCaptainId('') }} /> <span><strong>No captain</strong><small>Keep the draw purely random</small></span><i>✓</i></label><label className={captainMode === 'before' ? 'radio-card selected' : 'radio-card'}><input type="radio" checked={captainMode === 'before'} onChange={() => setCaptainMode('before')} /> <span><strong>Choose before</strong><small>Pick a captain from the roster</small></span><i>✓</i></label><label className={captainMode === 'after' ? 'radio-card selected' : 'radio-card'}><input type="radio" checked={captainMode === 'after'} onChange={() => setCaptainMode('after')} /> <span><strong>Choose after</strong><small>Pick one once teams are generated</small></span><i>✓</i></label><label className={captainMode === 'random' ? 'radio-card selected' : 'radio-card'}><input type="radio" checked={captainMode === 'random'} onChange={() => setCaptainMode('random')} /> <span><strong>Random captain</strong><small>Choose one automatically with the draw</small></span><i>✓</i></label>{captainMode === 'before' && <select className="captain-select" aria-label="Choose captain before randomization" value={captainId} onChange={(event) => setCaptainId(event.target.value)}><option value="">Choose a captain</option>{players.map((player) => <option key={player.id} value={player.id}>{player.name}</option>)}</select>}</fieldset></section>
      </div>
      <section className="cta-section"><div><span className="step">03</span><h2>Ready to play?</h2><p>{players.length ? `${players.length} players · ${teamCount} teams · ${distribution}` : 'Add your players to get started.'}</p></div><div className="balance-picker"><label htmlFor="balance-mode">Team randomizer</label><select id="balance-mode" value={balanceMode} onChange={(event) => setBalanceMode(event.target.value as TeamBalanceMode)}><option value="random">Pure random</option><option value="quality">Balance by quality</option></select></div><button className="primary-cta" disabled={isShuffling || !canGenerate} onClick={randomize}>{isShuffling ? 'Shuffling players…' : result ? 'Shuffle again' : 'Randomize teams'} <span>↗</span></button></section>
      {result && <section className="results" aria-live="polite"><div className="results-heading"><div><div className="eyebrow">MATCH TEAMS / GENERATED</div><h2>{matchName || 'Your teams are ready'}</h2>{matchDate && <p className="result-date">{new Date(`${matchDate}T00:00:00`).toLocaleDateString(undefined, { dateStyle: 'full' })}</p>}{captain && <p className="captain-result">★ Captain: <strong>{captain.name}</strong></p>}</div><div className="result-actions"><button onClick={copyTeams}>{copied ? 'Copied!' : 'Copy teams'}</button><button onClick={shareTeams}>Share teams</button><button onClick={() => window.print()}>Print</button></div></div>{captainMode === 'after' && <div className="after-captain"><label htmlFor="after-captain-select">Choose captain now</label><select id="after-captain-select" value={captainId} onChange={(event) => setCaptainId(event.target.value)}><option value="">Choose a captain</option>{players.map((player) => <option key={player.id} value={player.id}>{player.name}</option>)}</select></div>}<div className="summary-bar"><span><strong>{players.length}</strong> players</span><span><strong>{teamCount}</strong> teams</span><span><strong>{mode === 'auto' ? distribution : `${teamCount * playersPerTeam} assigned`}</strong> distribution</span><span className="assigned-status">● {result.unassigned.length ? `${result.unassigned.length} unassigned` : 'All players assigned'}</span></div><div className="team-grid">{result.teams.map((team, index) => <article className={`team-card team-${index % 4}`} key={team.id}><div className="team-card-heading"><span className="team-index">0{index + 1}</span><div><h3>{team.name}</h3><span>{team.players.length} players</span></div></div><ol>{team.players.map((player) => <li className={player.id === captainId ? 'captain-player' : ''} key={player.id}>{player.name}{player.id === captainId && <span className="captain-badge">CAPTAIN</span>}</li>)}</ol></article>)}</div>{result.unassigned.length > 0 && <div className="unassigned"><strong>Unassigned</strong>{result.unassigned.map((player) => <span key={player.id}>{player.name}</span>)}</div>}</section>}
    </>}</main><footer><span>KickSplit</span><span>Made for the players who showed up.</span></footer>
  </div>
}
export default App
